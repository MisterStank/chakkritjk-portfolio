# Resend API key — required for the contact form (https://resend.com/api-keys)
RESEND_API_KEY=

# Optional: GitHub personal access token to raise the API rate limit
# used by the "GitHub activity" section (public data only, no scopes needed)
GITHUB_TOKEN=
